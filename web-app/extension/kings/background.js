/**
 * 启动一个chrome.extension.onRequest事件监听器用来处理消息
 */
chrome.extension.onRequest.addListener(
  function(data){ 
	

	//chrome.tabs.create({url:'http://localhost:7070/ship/memberDaiFaOrder/add'});

	//var curTabID ;
	//chrome.tabs.query({active: true},function(arr){
	//	curTabID = arr[0].id;
	//});


});

package ship

class Market {

    static constraints = {
    }
    
     static mapping = {
        sort id: "asc" // or "asc"
    }
    
    static belongsTo = User
    static hasMany = [user:User]
    String market_name
}
